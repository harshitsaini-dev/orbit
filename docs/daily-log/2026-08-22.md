# 2026-08-22

## Previews, the right-click menu, and the roadmap for what was proposed

### Built

- A right-click menu for files and folders, with preview, download, star,
  rename and delete. Built from one place so the list and the grid cannot
  drift; an action the provider cannot do is disabled rather than hidden.
- The whole window is now the drop target, not just the file list. Drag enter
  and leave are counted rather than toggled, and `dragover` is cancelled
  window-wide so a missed drop cannot navigate the page away to the file.
- Orbit's own PDF viewer on pdf.js, replacing the iframe: page navigation,
  zoom, fit-to-width, fullscreen, and an inline password prompt for encrypted
  files instead of the browser's native box.
- Code and CSV previews: line numbers, syntax highlighting from a tokeniser
  written here rather than an editor pulled in, and a real delimited parser so
  a quoted comma cannot shift every column after it.
- Design files and disk images as their own categories, and eleven distinct
  file icons.

### Fixed

- The strict auth rate limit covered all of `/auth`, including the session read
  the app makes on every page load. A few open tabs used the budget and the
  punishment was being unable to sign in for the rest of the window. It now
  covers only the two endpoints that guess codes.
- pdf.js is pre-bundled at dev-server start. Discovered mid-session, Vite
  re-optimised and forced a reload that aborted whatever navigation was in
  flight — which showed up as eleven E2E failures with `net::ERR_ABORTED`.

### Documented

- `18-planned-capabilities.md`: the proposed feature list, each with what it
  costs and what the free tier will carry. Phases 12–22 on the board.
- ADR 0010 records the three that collide with the cost constraint: HLS
  transcoding declined in favour of the Range streaming that already meets the
  goal, P2P shipping with a fallback rather than paid TURN, and duplicate
  detection tiered by whether the hashes are actually comparable.

### Verified

- 359 unit tests, 138 Playwright tests across three viewports, headed.
- The PDF viewer was checked against generated fixtures, including an encrypted
  one: wrong password re-asks inline, correct password renders.

### Next

Phase 16 continues — fonts, markdown and archive browsing — then the directory
cache, which everything after it gets faster from.
